# BlogSidebar

**Import:** `@repo/ui/organisms` — `BlogSidebar`.

Vertical link rail for blog context (e.g. other posts in the same category). Pass **`title`** + **`items`** (`href`, `title`). Hide the organism when **`items`** is empty.
